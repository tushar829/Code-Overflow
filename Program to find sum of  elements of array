#include <iostream> 
using namespace std; 
int main() 
{ 
    int a[]={1,2,3,5,1,8,99};
    
    int n=sizeof(a)/sizeof(a[0]) ,  sum=0;
    
    for(int i=0;i<n;i++)
       sum+=a[i];
       
    cout<<"The sum of given array elements is :"<<sum;
    
    return 0; 
}
