#include<stdio.h>

int main()

{
    int n,s=0,i=0;

    printf("Enter the binary numbers:");
    scanf("%d",&n);

    while(n>0)
    {
        s=s+(n%10)*pow(2,i);
        n=n/10;
        i++;
    }

    printf("Entered number in decimal is %d",s);

    getch();
}
