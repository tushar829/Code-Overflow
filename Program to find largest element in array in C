#include <conio.h>
#include<stdio.h>
void main()
{
clrscr();
int arr[15] ,largest,i;
printf("enter the numbers");
for(i=0;i<=15;i++)
{
scanf("%d",&arr[i]);
}
for(i=0;i<=15;i++)
{
largest=arr[0];
if(largest<a[i])
largest=arr[i];
}
printf("largest of all is %d",largest);
getch();
}
