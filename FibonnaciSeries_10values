#include<stdio.h>
int fibo(int term);
int main()
{
	int i,term=10;
	for(i=0;i<term;i++)
	printf("%d ",fibo(i));
	
}

int fibo(int term)
{
	if(term==0)
	return 0;
	if(term==1)
	return 1;
	return fibo(term-1)+fibo(term-2);
}
