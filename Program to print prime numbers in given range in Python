def prime_numbers(range_number):
    prime_num_array = []
    for num in range(1, range_number): 
        for i in range(2, num):
            if num % i == 0:
                break
        else:
            prime_num_array.append(num)
    return prime_num_array


print(prime_numbers(50))            
