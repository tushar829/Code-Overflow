//Using C

#include<stdio.h>
int main()
{
  int a[100], b[100], size,i,k;
  scanf("%d",&size);
  for(i=0;i<size;i++)
  scanf("%d",&a[i]);
  k=size-1;
  for(i=0;i<size;i++)
  {
    b[i]=a[k];
    k--;
  }
  printf("Reverse array");
  for(i=0;i<size;i++)
  printf("%d",b[i]);
  return 0;
}
