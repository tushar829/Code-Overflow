#include <iostream>
using namespace std;

int main()
{
    unsigned int input;
    unsigned long long fact = 1;

    cout << "Enter a positive integer: ";
    cin >> input;

    for(int i = 1; i <=n; ++i)
    {
        fact *= i;
    }

    cout << "Factorial of " << input << " = " << fact;    
    return 0;
}
