    #include<iostream>
    #include<string.h>
    using namespace std;
    int main ()
    { 
        char str[50];
        int len;
        cout << "Enter a string : ";
        gets(str);
        len = strlen(str);
        cout << "Length of the string is : " << len;
        return 0;
        }
